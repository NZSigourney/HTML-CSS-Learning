// JavaScript Document

function username(){
	var arr = document.getElementsByTagName("input");
	var username = arr[0];
	var email = arr[1];
	
	if(username.length == 12){
		if(isNaN(username)){
			document
		}
	}
}